<p>📱</p><p><strong>STAR RANKER</strong></p><p>Mobile-First Engineering Prompt</p><p>Inspired by Polymarket &amp; Kalshi — Touch-Native Responsive Design for React + Tailwind v4</p><p><em>You are the senior frontend engineer on Star Ranker. The platform currently works well on desktop but is difficult to use on iPhone and Android. Your job is to make it feel as fluid and native on mobile as Polymarket and Kalshi — two of the best-designed prediction market apps on the web. Read this entire document before touching a file.</em></p><p><strong>Stack: React 19 + Vite, Tailwind CSS v4, Framer Motion, Zustand. Do not add new libraries. Do not change any backend code. This is a pure frontend mobile overhaul.</strong></p><h1><strong>0  — What Polymarket &amp; Kalshi Get Right on Mobile</strong></h1><p>Before writing a line of code, understand the design philosophy behind how these platforms handle small screens. Every decision you make should trace back to one of these principles.</p><table><tr><td><p><strong>Principle</strong></p></td><td><p><strong>What Polymarket Does</strong></p></td><td><p><strong>What Kalshi Does</strong></p></td></tr><tr><td><p><strong>Bottom Navigation</strong></p></td><td><p>Fixed bottom tab bar: Markets / Portfolio / Activity / Profile. Thumb zone navigation — never reach to the top.</p></td><td><p>Same pattern. Bottom tabs for primary navigation. Top header kept minimal with just logo + notifications.</p></td></tr><tr><td><p><strong>Cards Over Tables</strong></p></td><td><p>Every market is a tappable card. No data tables on mobile. Rank, odds, volume all summarised in one card row.</p></td><td><p>Compact cards with key metric (Yes/No price) prominent. Swipe gestures to reveal quick actions.</p></td></tr><tr><td><p><strong>Full-Screen Modals</strong></p></td><td><p>Trade panel opens as full-screen bottom sheet. Takes 85% of screen height. Large buttons, large inputs.</p></td><td><p>Order entry is a dedicated modal sheet. CTA button is full-width, high contrast, impossible to miss.</p></td></tr><tr><td><p><strong>Thumb Reachability</strong></p></td><td><p>All primary actions sit in the bottom 60% of screen. Nothing critical in top corners on mobile.</p></td><td><p>Key stats up top (read-only). All tap actions in bottom half. One-hand operable throughout.</p></td></tr><tr><td><p><strong>No Horizontal Scroll</strong></p></td><td><p>Content reflows into single column. Tables become stacked card lists. Charts truncate or hide on small screens.</p></td><td><p>Charts are simplified on mobile. Complex data collapses into summary numbers with 'View Details' tap.</p></td></tr><tr><td><p><strong>Large Touch Targets</strong></p></td><td><p>Buttons minimum 48px height. Spacing between tappable items at least 8px gap to prevent mis-taps.</p></td><td><p>Vote/trade actions are oversized — deliberately larger than desktop. Fat-finger friendly.</p></td></tr><tr><td><p><strong>Progressive Disclosure</strong></p></td><td><p>Show the most important number first. Everything else is behind a tap. Don't dump all data on screen at once.</p></td><td><p>Default view is simplified. 'Advanced' toggle reveals more. Mobile users want quick actions, not data.</p></td></tr><tr><td><p><strong>Visual Hierarchy</strong></p></td><td><p>Price/odds in large bold type. Supporting info in muted smaller text. One clear number that demands attention.</p></td><td><p>Strong colour coding: green = up, red = down. Instant visual comprehension without reading.</p></td></tr></table><h1><strong>1  — Global Mobile Foundation</strong></h1><p>These changes apply to the entire app. Do them first before touching any individual component.</p><h2><strong>1.1  Viewport &amp; Base CSS (index.html + index.css)</strong></h2><p><em>⚠  Check that index.html has the correct viewport meta tag. If it is missing or wrong, nothing else will work.</em></p><p>&lt;!-- index.html — must have this exact viewport tag --&gt;</p><p>&lt;meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"&gt;</p><p>/* index.css — global mobile resets */</p><p>*, *::before, *::after { box-sizing: border-box; }</p><p>html { -webkit-text-size-adjust: 100%; }  /* prevent iOS font inflation */</p><p>body {</p><p>  overscroll-behavior: none;        /* prevent bounce on iOS pull-down */</p><p>  -webkit-overflow-scrolling: touch; /* smooth momentum scroll */</p><p>  touch-action: manipulation;       /* remove 300ms tap delay everywhere */</p><p>}</p><p>/* Safe area insets — critical for iPhone notch &amp; home indicator */</p><p>:root {</p><p>  --safe-top:    env(safe-area-inset-top, 0px);</p><p>  --safe-bottom: env(safe-area-inset-bottom, 0px);</p><p>  --safe-left:   env(safe-area-inset-left, 0px);</p><p>  --safe-right:  env(safe-area-inset-right, 0px);</p><p>}</p><h2><strong>1.2  Tailwind Breakpoint Strategy</strong></h2><p>Star Ranker uses Tailwind v4. The mobile-first approach means: write the mobile style first (no prefix), then override for larger screens with md: and lg: prefixes. If you write desktop first and try to override downward, you will fight the framework the entire time.</p><p>/* WRONG — desktop first, hard to override for mobile */</p><p>&lt;div class="flex flex-row gap-8 p-8 md:flex-col md:gap-4 md:p-4"&gt;</p><p>/* CORRECT — mobile first, progressively enhanced for desktop */</p><p>&lt;div class="flex flex-col gap-4 p-4 md:flex-row md:gap-8 md:p-8"&gt;</p><p><em>ℹ  Every component you touch: read the existing classes first. Convert desktop-first patterns to mobile-first as you go.</em></p><h2><strong>1.3  Touch Target Minimum (Global Rule)</strong></h2><p>Every tappable element — buttons, vote arrows, navigation items, modal close buttons — must meet this minimum:</p><p>/* Add to your Tailwind component classes or global CSS */</p><p>.touch-target {</p><p>  min-height: 44px;   /* Apple HIG minimum */</p><p>  min-width:  44px;</p><p>  display: flex;</p><p>  align-items: center;</p><p>  justify-content: center;</p><p>}</p><p>/* Tailwind equivalent: class='min-h-[44px] min-w-[44px] flex items-center justify-center' */</p><h1><strong>2  — Replace Sidebar with Bottom Navigation (Polymarket Pattern)</strong></h1><p>This is the single highest-impact change. On desktop, your sidebar works. On mobile, a sidebar takes up 80% of the screen and requires a hamburger menu tap. Polymarket and Kalshi both abandoned the sidebar on mobile in favour of a bottom tab bar. You must do the same.</p><h2><strong>2.1  Create BottomNav.jsx (NEW)</strong></h2><p>Create this file at src/components/BottomNav.jsx. It renders only on mobile (hidden on md: and above). The desktop sidebar stays exactly as it is.</p><p>// src/components/BottomNav.jsx</p><p>import { useLocation, useNavigate } from 'react-router-dom';</p><p>import { BarChart2, Layers, Star, User } from 'lucide-react';</p><p>const tabs = [</p><p>  { label: 'Markets',   icon: BarChart2, path: '/markets'   },</p><p>  { label: 'Stake',     icon: Layers,    path: '/stake'     },</p><p>  { label: 'Rankings',  icon: Star,      path: '/'          },</p><p>  { label: 'Profile',   icon: User,      path: '/dashboard' },</p><p>];</p><p>export default function BottomNav() {</p><p>  const location = useLocation();</p><p>  const navigate = useNavigate();</p><p>  return (</p><p>    &lt;nav</p><p>      className='fixed bottom-0 left-0 right-0 z-50 md:hidden</p><p>                 bg-[#0D1B2A] border-t border-[#C9A84C]/30</p><p>                 flex items-stretch'</p><p>      style={{ paddingBottom: 'var(--safe-bottom)' }}</p><p>    &gt;</p><p>      {tabs.map(({ label, icon: Icon, path }) =&gt; {</p><p>        const active = location.pathname === path;</p><p>        return (</p><p>          &lt;button key={path}</p><p>            onClick={() =&gt; navigate(path)}</p><p>            className={`flex-1 flex flex-col items-center justify-center</p><p>                        py-3 gap-1 min-h-[56px] transition-colors</p><p>                        ${active ? 'text-[#C9A84C]' : 'text-gray-400 active:text-white'}`}</p><p>          &gt;</p><p>            &lt;Icon size={22} /&gt;</p><p>            &lt;span className='text-[10px] font-medium tracking-wide'&gt;{label}&lt;/span&gt;</p><p>          &lt;/button&gt;</p><p>        );</p><p>      })}</p><p>    &lt;/nav&gt;</p><p>  );</p><p>}</p><h2><strong>2.2  Add BottomNav to MainLayout.jsx</strong></h2><p>// In MainLayout.jsx — add BottomNav and bottom padding so content</p><p>// is not hidden behind the nav bar</p><p>import BottomNav from '../components/BottomNav';</p><p>return (</p><p>  &lt;div className='min-h-screen bg-[#0D1B2A]'&gt;</p><p>    {/* Existing desktop sidebar — add 'hidden md:flex' to hide on mobile */}</p><p>    &lt;Sidebar className='hidden md:flex' /&gt;</p><p>    &lt;main className='pb-[72px] md:pb-0'&gt;  {/* space for bottom nav */}</p><p>      {children}</p><p>    &lt;/main&gt;</p><p>    &lt;BottomNav /&gt;</p><p>  &lt;/div&gt;</p><p>);</p><h1><strong>3  — RankingTable: Desktop Table + Mobile Cards</strong></h1><p>The RankingTable is your most-used component. On desktop, a multi-column table makes sense. On mobile, it is illegible — columns overflow, text is too small, vote buttons are too close together. The Polymarket solution: two completely different render paths in one component.</p><h2><strong>3.1  Add a useIsMobile hook</strong></h2><p>// src/hooks/useIsMobile.js</p><p>import { useState, useEffect } from 'react';</p><p>export function useIsMobile(breakpoint = 768) {</p><p>  const [isMobile, setIsMobile] = useState(</p><p>    () =&gt; window.innerWidth &lt; breakpoint</p><p>  );</p><p>  useEffect(() =&gt; {</p><p>    const mq = window.matchMedia(`(max-width: ${breakpoint - 1}px)`);</p><p>    const handler = e =&gt; setIsMobile(e.matches);</p><p>    mq.addEventListener('change', handler);</p><p>    return () =&gt; mq.removeEventListener('change', handler);</p><p>  }, [breakpoint]);</p><p>  return isMobile;</p><p>}</p><h2><strong>3.2  Mobile Card layout in RankingTable.jsx</strong></h2><p>Import useIsMobile into RankingTable. Return the mobile card list when true, existing table when false.</p><p>// Inside RankingTable.jsx</p><p>import { useIsMobile } from '../hooks/useIsMobile';</p><p>export default function RankingTable({ items, categoryId }) {</p><p>  const isMobile = useIsMobile();</p><p>  if (isMobile) return &lt;MobileCardList items={items} categoryId={categoryId} /&gt;;</p><p>  return &lt;DesktopTable items={items} categoryId={categoryId} /&gt;;  // existing code</p><p>}</p><p>// New MobileCardList component (same file)</p><p>function MobileCardList({ items, categoryId }) {</p><p>  return (</p><p>    &lt;div className='flex flex-col gap-2 px-3 py-2'&gt;</p><p>      {items.map(item =&gt; (</p><p>        &lt;MobileItemCard key={item.id} item={item} categoryId={categoryId} /&gt;</p><p>      ))}</p><p>    &lt;/div&gt;</p><p>  );</p><p>}</p><p>function MobileItemCard({ item, categoryId }) {</p><p>  return (</p><p>    &lt;div className='bg-[#1E3A5F]/40 rounded-xl border border-white/10</p><p>                    flex items-center gap-3 px-4 py-3 active:bg-[#1E3A5F]/70</p><p>                    transition-colors cursor-pointer'&gt;</p><p>      {/* Rank badge */}</p><p>      &lt;span className='text-[#C9A84C] font-black text-lg w-6 text-center flex-shrink-0'&gt;</p><p>        {item.rank}</p><p>      &lt;/span&gt;</p><p>      {/* Item info */}</p><p>      &lt;div className='flex-1 min-w-0'&gt;</p><p>        &lt;p className='font-semibold text-white text-sm truncate'&gt;{item.name}&lt;/p&gt;</p><p>        &lt;p className='text-gray-400 text-xs'&gt;{item.subtitle}&lt;/p&gt;</p><p>      &lt;/div&gt;</p><p>      {/* Momentum indicator */}</p><p>      &lt;span className={`text-xs font-bold ${ item.momentum &gt; 0 ? 'text-green-400' : 'text-red-400' }`}&gt;</p><p>        {item.momentum &gt; 0 ? '▲' : '▼'} {Math.abs(item.momentum)}</p><p>      &lt;/span&gt;</p><p>      {/* Vote buttons — large touch targets */}</p><p>      &lt;div className='flex flex-col gap-1'&gt;</p><p>        &lt;button onClick={e =&gt; { e.stopPropagation(); handleVote(item.id, 'up'); }}</p><p>          className='min-h-[36px] min-w-[36px] flex items-center justify-center</p><p>                     rounded-lg bg-green-500/20 text-green-400 active:bg-green-500/40'&gt;</p><p>          ▲</p><p>        &lt;/button&gt;</p><p>        &lt;button onClick={e =&gt; { e.stopPropagation(); handleVote(item.id, 'down'); }}</p><p>          className='min-h-[36px] min-w-[36px] flex items-center justify-center</p><p>                     rounded-lg bg-red-500/20 text-red-400 active:bg-red-500/40'&gt;</p><p>          ▼</p><p>        &lt;/button&gt;</p><p>      &lt;/div&gt;</p><p>      {/* Stake CTA */}</p><p>      &lt;button onClick={() =&gt; openStakeModal(item)}</p><p>        className='min-h-[44px] px-3 rounded-xl bg-[#C9A84C] text-[#0D1B2A]</p><p>                   font-bold text-xs flex-shrink-0 active:opacity-80'&gt;</p><p>        STAKE</p><p>      &lt;/button&gt;</p><p>    &lt;/div&gt;</p><p>  );</p><p>}</p><h1><strong>4  — StakeModal: True Bottom Sheet on Mobile</strong></h1><p>The diagnostic says StakeModal already has some bottom-sheet logic. This section completes it properly — Kalshi-style, taking 85% of screen height, with drag-to-dismiss and a large thumb-friendly layout.</p><p><em>⚠  Do NOT break the existing desktop centered-modal behaviour. The mobile bottom-sheet is an additional render path, not a replacement of the existing code.</em></p><h2><strong>4.1  Bottom Sheet Container</strong></h2><p>// In StakeModal.jsx — wrap the existing modal content in this shell</p><p>import { useIsMobile } from '../hooks/useIsMobile';</p><p>import { motion, AnimatePresence } from 'framer-motion';</p><p>export default function StakeModal({ isOpen, onClose, item }) {</p><p>  const isMobile = useIsMobile();</p><p>  if (isMobile) {</p><p>    return (</p><p>      &lt;AnimatePresence&gt;</p><p>        {isOpen &amp;&amp; (</p><p>          &lt;&gt;</p><p>            {/* Backdrop */}</p><p>            &lt;motion.div</p><p>              initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}</p><p>              onClick={onClose}</p><p>              className='fixed inset-0 z-40 bg-black/60 backdrop-blur-sm'</p><p>            /&gt;</p><p>            {/* Sheet */}</p><p>            &lt;motion.div</p><p>              initial={{ y: '100%' }} animate={{ y: 0 }} exit={{ y: '100%' }}</p><p>              transition={{ type: 'spring', damping: 30, stiffness: 300 }}</p><p>              className='fixed bottom-0 left-0 right-0 z-50</p><p>                         bg-[#0D1B2A] rounded-t-3xl border-t border-[#C9A84C]/30</p><p>                         overflow-y-auto'</p><p>              style={{</p><p>                maxHeight: '88vh',</p><p>                paddingBottom: 'calc(var(--safe-bottom) + 16px)',</p><p>              }}</p><p>            &gt;</p><p>              {/* Drag handle */}</p><p>              &lt;div className='flex justify-center pt-3 pb-2'&gt;</p><p>                &lt;div className='w-10 h-1 rounded-full bg-white/20' /&gt;</p><p>              &lt;/div&gt;</p><p>              {/* Reuse existing StakeModalContent — no changes needed there */}</p><p>              &lt;StakeModalContent item={item} onClose={onClose} /&gt;</p><p>            &lt;/motion.div&gt;</p><p>          &lt;/&gt;</p><p>        )}</p><p>      &lt;/AnimatePresence&gt;</p><p>    );</p><p>  }</p><p>  // Desktop: existing centered modal code unchanged below</p><p>  return &lt;DesktopModal isOpen={isOpen} onClose={onClose} item={item} /&gt;;</p><p>}</p><h2><strong>4.2  Mobile Stake Input Layout</strong></h2><p>Inside StakeModalContent, make the following responsive changes for mobile:</p><ul><li>Amount input: full width, text-2xl, min-h-[56px], number pad friendly (inputMode='decimal')</li><li>Stake type tabs (Exact / Range / Directional): full-width flex row, equal columns, min-h-[48px] each</li><li>Fee disclosure line: prominent, not hidden — show '₦50 platform fee' in gold text</li><li>Confirm button: full width, min-h-[56px], text-lg, bottom of sheet — impossible to miss</li><li>Close button: top-right, min 44x44px, no tiny X icons</li></ul><h1><strong>5  — MarketPage &amp; Category Navigation</strong></h1><p>On desktop, category tabs run horizontally in a sidebar or top nav. On mobile, they need to be horizontally scrollable pills — exactly what Polymarket uses for their market categories.</p><h2><strong>5.1  Horizontally Scrollable Category Pills</strong></h2><p>// In MarketPage.jsx — replace desktop category nav with this on mobile</p><p>// Add 'overflow-x-auto scrollbar-none' to the container</p><p>&lt;div className='flex gap-2 overflow-x-auto scrollbar-none px-3 py-2</p><p>                 md:flex-wrap md:overflow-visible'&gt;</p><p>  {categories.map(cat =&gt; (</p><p>    &lt;button key={cat.id}</p><p>      onClick={() =&gt; setActiveCategory(cat.id)}</p><p>      className={`flex-shrink-0 px-4 py-2 rounded-full text-sm font-semibold</p><p>                  transition-colors whitespace-nowrap min-h-[36px]</p><p>                  ${activeCategory === cat.id</p><p>                    ? 'bg-[#C9A84C] text-[#0D1B2A]'</p><p>                    : 'bg-white/10 text-gray-300 active:bg-white/20'}`}</p><p>    &gt;</p><p>      {cat.icon}  {cat.name}</p><p>    &lt;/button&gt;</p><p>  ))}</p><p>&lt;/div&gt;</p><p>/* CSS to hide scrollbar while keeping scroll functionality */</p><p>.scrollbar-none { -ms-overflow-style: none; scrollbar-width: none; }</p><p>.scrollbar-none::-webkit-scrollbar { display: none; }</p><h2><strong>5.2  Sticky Mobile Header</strong></h2><p>On mobile, the top header should be minimal — just the Star Ranker logo + epoch countdown + wallet balance. Everything else moves to the bottom nav. Target height: 56px maximum.</p><p>// MobileHeader.jsx — shows only on mobile (md:hidden)</p><p>// Sticky at top. Contains:</p><p>// LEFT:   ★ Star Ranker logo (small)</p><p>// CENTER: Epoch countdown timer (EpochIndicator stripped to just the number)</p><p>// RIGHT:  Wallet balance + notification bell</p><p>// Height constraint is critical:</p><p>className='h-14 flex items-center justify-between px-4</p><p>           bg-[#0D1B2A]/95 backdrop-blur-sm</p><p>           border-b border-white/10 sticky top-0 z-30 md:hidden'</p><p>style={{ paddingTop: 'var(--safe-top)' }}</p><h1><strong>6  — DepositModal &amp; Paystack on Mobile</strong></h1><p>Paystack's payment popup works on mobile but needs specific handling. The DepositModal also needs the same bottom-sheet treatment as StakeModal.</p><h2><strong>6.1  Paystack Redirect Fallback</strong></h2><p><em>⚠  Paystack's JavaScript popup can be blocked by iOS Safari in some cases. Always have the redirect fallback ready.</em></p><p>// In DepositModal.jsx — after initializing transaction, prefer redirect on mobile</p><p>const isMobile = useIsMobile();</p><p>const handlePayment = async () =&gt; {</p><p>  const { data } = await api.post('/api/payments/initialize', { amount });</p><p>  if (isMobile) {</p><p>    // Redirect to Paystack hosted page — more reliable on iOS Safari</p><p>    window.location.href = data.authorization_url;</p><p>  } else {</p><p>    // Desktop: use Paystack popup</p><p>    const handler = PaystackPop.setup({ ... });</p><p>    handler.openIframe();</p><p>  }</p><p>};</p><p>// After Paystack redirects back to your callback URL,</p><p>// verify the transaction and update Zustand balance.</p><p>// Your /api/payments/verify/:ref route already handles this.</p><h2><strong>6.2  DepositModal as Bottom Sheet</strong></h2><p>Apply the exact same bottom-sheet pattern from Section 4 to DepositModal. The deposit form should have:</p><ul><li>Full-width NGN amount input with a large number keyboard (inputMode='numeric')</li><li>Preset quick-amount buttons: ₦1,000 | ₦2,500 | ₦5,000 | ₦10,000 — large, tappable, full row</li><li>Currency conversion line: '≈ $X.XX USD' in muted text below the input</li><li>'Fund Wallet' CTA: full width, min-h-[56px], gold background</li></ul><h1><strong>7  — Dashboard: Mobile Stats Layout</strong></h1><p>The dashboard on mobile should follow Kalshi's pattern: a vertical stack of metric cards, each full width, with the key number large and prominent. No sidebars, no horizontal tables.</p><h2><strong>7.1  Stats Cards</strong></h2><p>// DashboardPage.jsx — mobile layout</p><p>// Replace any flex-row or grid layouts with a single column on mobile</p><p>// Stats grid: 2 columns on mobile, 4 on desktop</p><p>&lt;div className="grid grid-cols-2 md:grid-cols-4 gap-3 p-4"&gt;</p><p>  {stats.map(stat =&gt; (</p><p>    &lt;div key={stat.label} className="bg-[#1E3A5F]/40 rounded-2xl p-4 border border-white/10"&gt;</p><p>      &lt;p className="text-gray-400 text-xs mb-1"&gt;{stat.label}&lt;/p&gt;</p><p>      &lt;p className="text-white font-black text-2xl"&gt;{stat.value}&lt;/p&gt;</p><p>      &lt;p className={`text-xs mt-1 ${stat.change &gt; 0 ? "text-green-400" : "text-red-400"}`}&gt;</p><p>        {stat.change &gt; 0 ? '▲' : '▼'} {stat.changeLabel}</p><p>      &lt;/p&gt;</p><p>    &lt;/div&gt;</p><p>  ))}</p><p>&lt;/div&gt;</p><h1><strong>8  — iOS Safari Specific Fixes</strong></h1><p>iOS Safari has several quirks that break web apps in ways Android Chrome does not. Fix all of these before testing on an iPhone.</p><h2><strong>8.1  Input Zoom Prevention</strong></h2><p><em>⚠  iOS Safari automatically zooms in when an input has font-size less than 16px. This is extremely jarring. Fix it globally.</em></p><p>/* index.css — prevent iOS input zoom */</p><p>input, select, textarea {</p><p>  font-size: 16px !important;  /* minimum to prevent iOS zoom */</p><p>}</p><p>/* Then use transform: scale() if you need visually smaller inputs */</p><h2><strong>8.2  Position Fixed + Keyboard</strong></h2><p><em>⚠  On iOS, when the keyboard opens, elements with position:fixed can jump around or disappear. Affect your bottom nav and modals.</em></p><p>// In your bottom sheet / modal: use visualViewport API to handle keyboard</p><p>useEffect(() =&gt; {</p><p>  const vv = window.visualViewport;</p><p>  if (!vv) return;</p><p>  const handler = () =&gt; {</p><p>    // When keyboard opens, vv.height shrinks</p><p>    document.documentElement.style.setProperty(</p><p>      '--viewport-height', `${vv.height}px`</p><p>    );</p><p>  };</p><p>  vv.addEventListener('resize', handler);</p><p>  return () =&gt; vv.removeEventListener('resize', handler);</p><p>}, []);</p><p>/* In CSS, use var(--viewport-height) instead of 100vh for modals */</p><p>.modal { height: var(--viewport-height, 100vh); }</p><h2><strong>8.3  Scroll Lock When Modal Open</strong></h2><p>// Prevent background scroll when StakeModal or DepositModal is open</p><p>// Add this to every modal's open/close logic:</p><p>useEffect(() =&gt; {</p><p>  if (isOpen) {</p><p>    document.body.style.overflow = 'hidden';</p><p>    document.body.style.position = 'fixed';  // critical for iOS</p><p>    document.body.style.width = '100%';</p><p>  } else {</p><p>    document.body.style.overflow = '';</p><p>    document.body.style.position = '';</p><p>    document.body.style.width = '';</p><p>  }</p><p>  return () =&gt; {</p><p>    document.body.style.overflow = '';</p><p>    document.body.style.position = '';</p><p>    document.body.style.width = '';</p><p>  };</p><p>}, [isOpen]);</p><h2><strong>8.4  localStorage in Private Mode</strong></h2><p><em>⚠  iOS Safari Private Mode throws an exception on localStorage.setItem(). The app must not crash.</em></p><p>// Wrap all localStorage usage in a try/catch utility:</p><p>export const storage = {</p><p>  get: (key) =&gt; { try { return localStorage.getItem(key); } catch { return null; } },</p><p>  set: (key, val) =&gt; { try { localStorage.setItem(key, val); } catch {} },</p><p>  del: (key) =&gt; { try { localStorage.removeItem(key); } catch {} },</p><p>};</p><p>// Replace all direct localStorage calls in the codebase with storage.get/set/del</p><h1><strong>9  — Execution Order</strong></h1><p>Follow this sequence. Each step builds on the last.</p><table><tr><td><p><strong>#</strong></p></td><td><p><strong>Task</strong></p></td><td><p><strong>Time</strong></p></td><td><p><strong>Impact</strong></p></td></tr><tr><td><p><strong>1</strong></p></td><td><p>Fix viewport meta tag + global CSS resets (Section 1.1)</p></td><td><p>20 mins</p></td><td><p>🔴 Critical foundation</p></td></tr><tr><td><p><strong>2</strong></p></td><td><p>Add useIsMobile hook (Section 3.1)</p></td><td><p>15 mins</p></td><td><p>Required by all below</p></td></tr><tr><td><p><strong>3</strong></p></td><td><p>Build BottomNav.jsx + hide sidebar on mobile (Section 2)</p></td><td><p>1.5 hrs</p></td><td><p>🔴 Highest UX impact</p></td></tr><tr><td><p><strong>4</strong></p></td><td><p>RankingTable → MobileCardList (Section 3.2)</p></td><td><p>2 hrs</p></td><td><p>🔴 Core feature usable</p></td></tr><tr><td><p><strong>5</strong></p></td><td><p>StakeModal bottom sheet (Section 4)</p></td><td><p>1.5 hrs</p></td><td><p>🔴 Revenue critical</p></td></tr><tr><td><p><strong>6</strong></p></td><td><p>Fix iOS Safari: input zoom, scroll lock, localStorage</p></td><td><p>1 hr</p></td><td><p>🔴 iPhone works</p></td></tr><tr><td><p><strong>7</strong></p></td><td><p>Category pills horizontal scroll (Section 5.1)</p></td><td><p>45 mins</p></td><td><p>High polish</p></td></tr><tr><td><p><strong>8</strong></p></td><td><p>MobileHeader — minimal sticky header (Section 5.2)</p></td><td><p>1 hr</p></td><td><p>High polish</p></td></tr><tr><td><p><strong>9</strong></p></td><td><p>DepositModal bottom sheet + Paystack redirect (Section 6)</p></td><td><p>1.5 hrs</p></td><td><p>Deposits work on iOS</p></td></tr><tr><td><p><strong>10</strong></p></td><td><p>Dashboard stats cards mobile layout (Section 7)</p></td><td><p>1 hr</p></td><td><p>Dashboard usable</p></td></tr><tr><td><p><strong>11</strong></p></td><td><p>Full test on real iPhone AND Android Chrome</p></td><td><p>1 hr</p></td><td><p>Confidence to launch</p></td></tr></table><h1><strong>10  — Mobile Testing Checklist</strong></h1><p>Test on a REAL iPhone and a REAL Android device. Browser DevTools mobile emulation misses iOS-specific bugs. Do not trust emulation alone.</p><p><strong>Navigation</strong></p><ul><li>Bottom nav visible on iPhone Safari at 375px width</li><li>All 4 tabs navigate correctly, active state highlights in gold</li><li>Desktop sidebar still visible and working at 1280px</li><li>No horizontal scroll on any page</li></ul><p><strong>RankingTable</strong></p><ul><li>Cards show on iPhone — no table overflow</li><li>Vote buttons are easily tappable without mis-tapping adjacent buttons</li><li>STAKE button opens bottom sheet, not a tiny centered modal</li><li>Item names truncate correctly without breaking layout</li></ul><p><strong>StakeModal</strong></p><ul><li>Slides up from bottom on iPhone with spring animation</li><li>Drag handle visible at top of sheet</li><li>Tap backdrop dismisses the sheet</li><li>Amount input doesn't cause page zoom on iOS</li><li>Confirm button is full width and clearly visible above keyboard</li></ul><p><strong>Deposits</strong></p><ul><li>Paystack redirects to hosted page on mobile (not popup)</li><li>After payment completes, balance updates correctly on return</li></ul><p><strong>General</strong></p><ul><li>No JavaScript errors in Safari console</li><li>localStorage failure in Private Mode does not crash the app</li><li>Home indicator area (iPhone bottom bar) not covered by content</li><li>Notch area (iPhone top) not covered by content</li></ul><p><strong>📱  Mobile Done Right  📱</strong></p><p><em>When a user can stake on their iPhone at 11pm with one thumb, you win.</em></p><p>© 2026 Star Ranker Technologies Limited  ·  Confidential Engineering Document</p>